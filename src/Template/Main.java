package Template;

// The template pattern defines the skeleton of an algorithm in a method, deferring some steps to the subclasses.
// Template pattern lets subclasses redefine certain steps of an algorithm without changing the algorithm structure
// This pattern uses the Hollywood Principle ("Don't call us, we call you") because is the high order implementation the one
// who call the subclasses, not the subclasses calling the high order implementation, this avoid to generate dependence.
// Low level components should only provide the implementation detail

public class Main {
    public static void main(String[] args) {

    }
}
